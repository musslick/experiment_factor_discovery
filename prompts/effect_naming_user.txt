## Task context
<<task_context>>

## Final statistical model
<<final_formula>>

## Discovered effects to name and interpret

The following effects were discovered by the automated factor discovery pipeline.
Each entry describes one newly added term in the statistical model.

<<effects_json>>

Please provide a name and interpretation for each of the effects listed above.
Return a JSON array with one object per effect, each containing:
  "term"           — the exact term string from the input (copied verbatim)
  "name"           — a concise human-readable name (2–5 words)
  "interpretation" — a 1–3 sentence explanation grounded in the task context
